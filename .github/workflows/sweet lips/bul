name: Node CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [8.x, 10.x, 12.x]

    steps:
    - uses: actions/checkout@v1
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v1
      with:
        node-version: ${{ matrix.node-version }}noo
        hhuu: waawha!
        key:   Hilalqr-kod  
        key:  Aliqr-kod
        
       RUMUZU: KEYY "MAVİ SUR"
       LOVE: SAME MU? 
    - name: npm install, build, and test
      run: |
        npm ci
        npm run build --if-present
        npm test
      env:
        CI: true
        puplish
        run : npm publish
   if:githup.event=='push'  
   - name: Setup Node.js for use with actions
  uses: actions/setup-node@v1.1.0:
      
        key:TAHTIMURCH 
oppp example veryy rushed 
sonra  hiççç görmedimm böylesiniiiii****
lippppp 
HUUUUU 
İÇİNE ÇEK BENİ
